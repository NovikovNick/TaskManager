version: "3"
services:
  running-list-db:
    build: ./docker/db/postgres
    expose:
      - 5432
    environment:
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - ./data:/var/lib/postgresql/data/pgdata
    ports:
      - 5432:5432
    network_mode: "host"
